[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "event-information-platform"
version = "0.0.0"
description = "Event Information Gathering and Dissemination platform scaffold"
requires-python = ">=3.11"
