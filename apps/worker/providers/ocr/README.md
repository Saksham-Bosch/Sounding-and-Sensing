# OCR Provider Interface

This folder is reserved for OCR and document-processing provider interfaces and adapters.

Phase 0 intent:

- Keep provider behavior interface-driven.
- Avoid implementation tied to unapproved external models.
