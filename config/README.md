# Configuration

This folder holds configuration guidance and safe example files.

- `local.example.yaml` shows non-secret local defaults.
- Real local configuration should stay ignored when it contains environment-specific values.
